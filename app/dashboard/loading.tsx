import React from "react";

const LoadingDashboard = () => {
	return (
		<div className="flex flex-col items-center justify-center h-[calc(100vh-100px)] border">
			<h1>Relax, we are working on it.</h1>
		</div>
	);
};

export default LoadingDashboard;
